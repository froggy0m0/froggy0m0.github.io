---
layout: post
title: RPAC python모듈 구성
categories: Project
message: 라즈베리파이를 이용한 식물재배기 코드
order: 1
---

## main.py

부팅시 작동할수있게 profile.d 에 저장

__default로 모든 센서 off 함수???__

### insert.py

1. 라즈베리파이 센서 값을 firestore에 주기적인 데이터값저장

1. 사용자 입력에의한 수동 측정값 저장

1. camera 데이터베이스저장

### management.py

수동작동

led,pump,pen,

camera 수동작동

### reserved.py

안드로이드 or 웹에서 받아온 데이터, 혹은 시스템에서 제공하는 값을 이용한 자동화 시스템 management.py 와 비슷



---

#### RPAC.py

import RPAC

insert.py, management.py, reserved.py 에 사용될 메소드구현

​    

##### 공통

1. db객체 생성( \__init__ 함수로 ?)

​    

##### insert.py

1. 데이터값 저장 함수

​    

##### management.py & reserved.py

1. 데이터 검색 반환함수(슬라이싱보다는 받아온 문자열을 딕셔너리로 변환해서 키값으로 매핑 ?)

   

2. led,pump,pen, on/off 함수

   while함수를통해 사용자가모드 확인  수동모드 OR 자동모드 

   1. 수동모드
      1. on

         1. python 상태값이 'off' && DB 상태값이 'on'이면 on함수 실행

         2. python 상태값을 'on'으로 변환

               

      2. off

         1. python 상태값이 'on' && DB 상태값이 'off'이면 off 함수 실행
         2. python 상태값을 'off'으로 변환

   ​    

    1. 자동모드

        1. DB에 설정되어있는 값을 통해 작동

            1. PUMP

               management.py 의 while문 밖에서 따로 함수설정 ex)매일오후12시 물주기

               python의 schedule 함수 ?  everyday.attoday("12:00")

               python의 os 함수? crnotab 자동등록? 삭제 ?

           1. PEN

              일정습도 이상이면 PEN 작동

           2. LED

              일정온도 이하면 LED 작동

       

   ```python
   while(True)
       if(led스위치 == True)
        on 
   	병렬처리???
        
       elif(led스위치 == False)
        off
           
       if(pump스위치 == True)
       on
       
       elif(pump스위치 == False)
       off 
       ##이런식으로 led,pump,pen은 같은 while문에서 동작하게 작동 >> 병렬처리
   ```

3. 사용자의 셋팅이 수동작동인지 자동작동인지 반환해주는 함수(데이터베이스의 문서값에서 True or False로 판단)

   

4. 사용자의 값이 변했을때 체크해주는함수(date값을 저장해놓고 date값이 변하면 True)


```python
class Person:
    def __init__(self):
        self.ht = False
        self.pump = False
        self.pen = False
        self.led = False

    def ht_switch(self):
        if self.ht :
            self.ht = False
            print("ht를 off")
        else:
            self.ht = True
            print("ht를 on")

    def pump_switch(self):
        if self.pump :
            self.pump = False
            print("pump를 off")
        else:
            self.pump = True
            print("pump를 on")

    def pen_switch(self):
        if self.pen :
            self.pen = False
            print("pen를 off")
        else:
            self.pen = True
            print("pen를 on")

    def led_switch(self):
        if self.led :
            self.led = False
            print("led를 off")
        else:
            self.led = True
            print("led를 on")


    def ht_status(self):
        print("ht는 on상태입니다" if self.ht else "ht는 off상태입니다")
    def pump_status(self):
        print("pump는 on상태입니다" if self.pump else "pump는 off상태입니다")
    def pen_status(self):
        print("pen는 on상태입니다" if self.pen else "pump는 off상태입니다")
    def led_status(self):
        print("led는 on상태입니다" if self.led else "led는 off상태입니다")

```

```python
import person

pp = person.Person()

while(True):
    a = input()
    if a == "ht":
        pp.ht_switch()
    if a == "pump" :
        pp.pump_switch()
    if a == "pen" :
        pp.pen_switch()
    if a == "led" :
        pp.led_switch()
    if a == "1" :
        pp.ht_status()
    if a == "2" :
        pp.pump_status()
    if a == "3" :
        pp.pen_status()
    if a == "4" :
        pp.led_status()

    if pp.pump == True:
        print("pump 켜짐@@#@#@")
```



