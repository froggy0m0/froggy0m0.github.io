---
layout: post
title: Python 끄적끄적...
categories: Memo
message: Python 코드내용..

---



1. ## 딕셔너리 생성

   ```python
   count = {x:0 for x in range(0,10)}
   ```

   컴프리핸션을 이용한 딕셔너리 생성     

   ​    

   

2. ## eval 함수

   ```python
   num = []
   s = list(str(25))
   s='*'.join(s)
   num.append((eval(s)))
   print(num)
   
   >>> [10]
   ```

   ​    

3. ## swap 기능

   ```python
   x,y = y,x
   ```

   

4. ## join함수

   ``` python
   li = ['a','b','c',]
   li = '_'.join(li)
   print(li)
   
   >>> "a_b_c"
   ```

   

5. ## 문자열 한문자씩 나누기

   ```python
   li = 'ABCDE'
   li = list(li)
   print(li)
   
   >>> ['A', 'B', 'C', 'D', 'E']
   ```

   

6. 

7. 

