---
layout: post
title: FSB(Format String Bug) 32bit-(2)
categories: Pwnable
message: CS에 대한 기초지식이 부족해서그런지 FSB에 시간을 꽤 투자했는데 그냥 가긴 아쉬워서 정리하는 글...
---

__OS : Ubuntu 18.04__

## FSB(Format String Bug)-2

### example4

```c
// Name : example4
// Compaile : gcc -o example4 example4.c -m32 -mpreferred-stack-boundary=2 -fno-stack-protector -z execstack -no-pie

#include<stdio.h>


void get_shell()
{
    system("/bin/sh");
}

int main(void)
{   
    int val = 0xfff; // 0xfff = 4095
    char buf[20] ={0,};

    printf("val = %d\n", val);
    printf("val_addr = %p\n", &val);


    gets(buf);
    printf(buf);
    
    if(val == 0xffe) // 0xffe = 4094
    {
        get_shell();
    }
    printf("\nval = %d\n", val);

}
```



<figure>
    <img src="https://Froggy0m0.github.io/assets/img/Pwnable/2022-01-10-fsb_32bit2-1.png">
    <figcaption>Fig 1. example4 메모리 보호기법</figcaption>
</figure>



**example4** 는 val 변수의 주소를 출력하고 사용자로부터 입력을 받고 출력해주고 val 변수가 'B' 이면 get_shell 함수를 호출한다.

쉘을 얻기 위한 시나리오로는 val변수의값을 0xffe 로 변경 시켜 쉘을 얻거나

PIE는 적용돼있지 않고 Partial RELRO가 적용돼있기 때문에 get_shell함수의 주소를 얻어 print의 GOT 에 overwrite하여 printf함수 대신 get_shell함수를 실행시킬 수 있을 것이다.

<figure>
    <img src="https://Froggy0m0.github.io/assets/img/Pwnable/2022-01-10-fsb_32bit2-2.png">
    <figcaption>Fig 2. example4 실행</figcaption>
</figure>

__Fig 2__ 를 통해 **val** 변수 주소(0xffffd3c0)가 고정돼있는 것을 확인해 볼 수 있다.

먼저 `AAAA %p %p %p %p`를 입력하여 esp와의 offset을 확인해보았다.

실행 결과 AAAA(0x41414141)은 `%p`로 `%1$p`에 위치해있다.

#### val 변수값 변경

먼저 val 변수값을 변경하여 쉘을 얻어보겠다.

 AAAA의 위치에 val변수의 주소를 넣고 %[숫자]c를 사용하여 '0xffe'의 (10진수값 4094) 만큼 채워놓은후 $[offset]$n으로 val 변수를 '0ffe'로 pwntools 을 사용하여 다음과 같이 채워놓을수 있을 것이다. 

payload = val주소(4byte) + %4090c + %1$n

여기서 %4090c인 이유는 앞에 val주소가 4byte로 이미 출력되어있어 4094 - 4 를 계산한 %4090c로 작성하였다.

```
```

